---
layout: single
title: "인터넷 동작방식"
categories: Basic
tag: [Web]
sidebar_main: true
toc: true
toc_sticky: true
---

최초의 데이터 교환은 스탠포드 대학교의에서 인터넷의 전 모델인 알파넷(ARPANET)으로 전송한 메세지로 LOGIN 명령이다.
하지만 이 초기 네트워크는 LO 두글자를 보내고나면 항상 접속이 끊어져 신뢰성이 없었다.
소련의 해킹 공격으로인한 오프라인 상태에서도 데이터를 교환할 수 있는 방법을 찾고있던 미국의 네트워크 엔지니어들은 컴퓨터들 간의 신뢰할 수 있는 정보 교환을 보장하는 TCP를 개발했다.

# TCP (Transmission Control Protocol)

TCP는 인터넷 프로토콜 스위트로 총칭되는 약 20개의 네트워크 프로토콜 줄 하나이다.
컴퓨터가 TCP로 다른 기게에 메세지를 보낼떄 메세지는 목적지인 주소를 가지고 최종목적지를 향해 전송되는 데이커패킷(data packet)으로 분할된다.
인터넷을 구성하는 컴퓨터는 전체 메세지를 처리하지않고 패킷을 목적지로 보낸다.
수신 시스템이 패킷을 받으면 프로토콜은 각 패킷의 순서에따라 패킷들을 재사용할 수 있는 순서로 조립한다.
수신자는 수신을 패킷을 받을때마다 수신을 보내고, 수신자가 패킷을 승인하지 못하면 송신자는 다른 네트워크 경로를 따라 해당 패킷을 제전송한다.

인터넷의 성장에따라 TCP는 checksum 기능을 제공하여 송신자의 데이터 전송 속도를 소비 속도에따라 우선 조정 가능하게되었다.

> **checksum(체크섬)**
>
> packet이 수신자가 데이터 손상을 감지하고 패킷으 재전송해야 하는 지 여부를 결정할 수 있게 도와주는것

TCP는 전달 보증떄문에 가장 일반적인 프로토콜로 남아있지만, 오늘날에는 UDP도 자주 사용되고 있다.

> **_UDP_(User Datagram Protocol)**
>
> - 데이터가 일정한 속도로 스트리밍 될 수 있도록 패킷을의도적으로 삭제하는 새로운 프로토콜
> - 소비자들이 네트워크가 혼잡해질떄 Feed(피드)를 지연하는 것보다 프레임을 조금 떨어뜨리는 것을 더 선호하기떄문에 Live Video Streaming에 자주 사용된다.

# IP (Internet Protocol)

인터넷의 데이터 패킷은 인터넷 프로토콜(IP) 주소로 전송되며, 개별 인터넷에 연결된 컴퓨터에 할당된 번호로 전송한다.
각 IP 는 고유해야하므로, 새로운 IP 주소는 구조화된 방식으로 발급한다.

국제 도메인 관리기구(ICANN, Internet Coporation for Assigned Names and Number)는 가장 높은 수준에서 지역 당국에 IP 주소의 블록을 할당한다.
지역 당국은 인터넷 서비스 제공업체(ISP, Internet Service Provider)와 해당 지역 내 호스팅 회사에 주소 블록을 부여한다.
브라우저를 인터넷에 연결하면 ISP 는 몇 달간 고정된 상태를 유지(ISP는 주기적으로 IP 주소를 순회한다)하는 IP주소를 컴퓨터에 할당한다.
인터넷에서 콘텐츠를 호스팅하는 회사들도 네트워크에 접속하는 각 서버의 IP주소를 할당받는다.

> IP 주소
>
> 일반적으로 2진수로 2^32(4294,967,296) 의 주소를 허용하는 IPv4 구문으로 작성된다.
> ex) google의 도네임 서버주소 8.8.8.8
> IPv4 주소는 지속 가능하지않은 속도로 사용되기떄문에 콜론으로 구분된 4개의 16진수 숫자의 8개 그룹 (fe80::8274:36c5:3197:7a3b%3)으로 대표디는 더 많은 연결장치를 허용하려고하ㅡㄴ 인터넷이 IPv6 주소로 이동하고 있다.

# DNS (Domain Name System)

# Application Layered Protocol

TCP나 UDP 위에 구축되는 protocol을 애플리케이션 계층 프로토콜이라 한다.

### HTTP (HyperText Transfer Protocol)

웹 서버는 HTTP를 사용하여 웹 페이지와 웹 페이지의 자원을 웹브라우저와 같은 사용자 에이전트로 전송한다.

> **HTTP 요청**
>
> - Method
> - UURL
> - Headers
> - Body

```bash
GET http://example.com

```

### stateful connection

###
